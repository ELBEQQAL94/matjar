import styles from "../styles/content.module.css";

const Content = () => {
    return (
        <section className={styles.contentContainer}>
            <h1>المنتجات الأكثر مبيعًا</h1>
        </section>
    );
};

export default Content;
